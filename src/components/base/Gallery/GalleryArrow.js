import React from 'react';

const GalleryArrow = ({
  className,
  onClick,
}) => {
  return (
    <div
      className={className}
      onClick={onClick}
    />
  );
}

export default GalleryArrow;
